services:
  - type: web
    name: sitebus
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn sitebus.wsgi:application
    plan: free
    autoDeploy: true
